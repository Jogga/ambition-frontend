describe('records controller', function () {
    'use strict';

    beforeEach(module('amb.record.RecordController'));
    beforeEach(inject());

    it('load RecordController module', function () {

    });
});
